@charset "utf-8";
@import '_partial/base/ress'

/* Variables
--------------------------------------------------------------------------------------------------------*/

@import '_partial/variables'

media(bpName)
    for name in bpName
        if name == 'spm'
            @media only screen and (max-width: spm-width)
                {block}
        if name == 'sp'
            @media only screen and (max-width: sp-width)
                {block}
        if name == 'tabsp'
            @media only screen and (max-width: pc-width - 1px)
                {block}
        if name == 'tab'
            @media only screen and (min-width: sp-width + 1px) and (max-width: pc-width - 1px)
                {block}
        if name == 'pc'
            @media only screen and (min-width: pc-width)
                {block}


html
    scroll-behavior: smooth

body
    background: var(--background-color)
    font-family: "Hiragino Kaku Gothic Pro","ヒラギノ角ゴ Pro W3","メイリオ","Meiryo","ＭＳ Ｐゴシック","Helvetica Neue","Helvetica","Arial",sans-serif


::selection
    background: #b3d4fc
    color: #444


@keyframes spinner
    from
        transform rotate(0deg)
    to
        transform rotate(360deg)

.loading
    position relative

.loading::before
    --spinner-size: 36px
    content ""
    position absolute
    top calc(50% - 0.5*var(--spinner-size))
    left calc(50% - 0.5*var(--spinner-size))
    z-index 0
    width var(--spinner-size)
    height var(--spinner-size)
    border-radius 50%
    border 2px solid rgb(204,204,204)
    border-top-color rgb(51,51,51)
    animation spinner .5s cubic-bezier(.42, .61, .58, .41) infinite



/* partial
--------------------------------------------------------------------------------------------------------*/

// ベース
@import '_partial/base/styles/atom-one-dark.css'
@import '_partial/pixel-viewer'

//ヘッダー
@import '_partial/header'

// メイン
@import '_partial/container'
@import '_partial/breadcrumb'
@import '_partial/post-list'
@import '_partial/pager'

// 記事
@import '_partial/post'
@import '_partial/content'


// サイドバー
@import '_partial/aside'

//フッター
@import '_partial/footer'
