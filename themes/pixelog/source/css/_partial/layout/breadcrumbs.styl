.breadcrumbs
    height: 56px
    margin 0 0 16px 0
    line-height 56px
    background var(--background-sub-color)
    font-size 14px
    box-shadow boxshadow
    border-radius 4px

    &__list
        display flex
        list-style none
        overflow-x auto
        overflow-y hidden
        scroll-snap-type x mandatory
        position relative

    &__item
        position relative
        padding 0 24px
        flex-shrink 0
        scroll-snap-align start

        &-link
            text-decoration underline
            transition .1s color ease

            &:hover
                color var(--text-hover-color)

        &:last-child
            width 100%
            min-width 10em
            padding 0 16px 0 24px
            flex-shrink 1
            text-overflow ellipsis
            white-space nowrap
            overflow hidden
            font-weight bold

            & > a
                color var(--text-accent-color)
                text-decoration none
                pointer-events none

        &:first-child
            padding 0 24px 0 16px

        &:not(:last-child)
            &::before
                content ""
                display block
                width 0
                height 0
                border 30px solid transparent
                border-left 16px solid var(--background-sub-color)
                position absolute
                top -2px
                right -35px
                z-index 2

            &::after
                content ""
                display block
                width 0
                height 0
                border 30px solid transparent
                border-left 16px solid var(--border-color)
                position absolute
                top -2px
                right -36px
                z-index 1
