.header
    background var(--bg-primary)
    box-shadow var(--header-shadow)

    &__logo // h1のデフォルトfont-sizeを上書きするため
        font-size 24px
        +media(sp)
            font-size 20px


    &__svg
        margin auto
        color #fff

    &__link
        display block
        padding 4em 0
