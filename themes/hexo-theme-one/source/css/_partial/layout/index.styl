.index
    &__list
        display grid
        margin var(--space)
        gap 1.5em

    &__item
        display block
        line-height 1.5

    &__link
        word-break break-all

    &__date
        display block
        color var(--text-sub-color)
        font-size var(--size-s2)
