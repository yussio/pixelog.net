<%
var tagName;
if(is_home()){
  tagName = 'h1';
}else{
  tagName = 'div';
}
%>

<header class="header">
  <<%= tagName %> class="header__logo">
      <a class="header__link" href="<%= url_for('/') %>">
          <%= config.title %>
      </a>
  </<%= tagName %>>
</header>
